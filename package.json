{
  "name": "portfolio02",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --strictPort --host",
    "build": "tsc && vite build --minify esbuild",
    "preview": "vite preview --strictPort --port 5173"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.2.0"
  }
}
